name: creating

on: [push, pull_request, issues]

jobs:
  creating:
    runs-on: ubuntu-latest
    steps:
    - uses: https://github.com/AkhilAchanta/AkhilAchanta.git/action-create-branch@v2.0.1
      env:
       GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      with:
       branch: 'feature'
